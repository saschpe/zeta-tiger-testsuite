<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="LOG_DIR" value="${LOG_DIR:-${java.io.tmpdir}/zeta-testsuite-logs}"/>
    <property name="CONSOLE_LOG_LEVEL" value="${CONSOLE_LOG_LEVEL:-WARN}"/>
    <property name="FILE_LOG_LEVEL" value="${FILE_LOG_LEVEL:-INFO}"/>
    <property name="TIGER_LOG_LEVEL" value="${TIGER_LOG_LEVEL:-INFO}"/>
    <property name="TIGER_PROXY_LOG_LEVEL" value="${TIGER_PROXY_LOG_LEVEL:-INFO}"/>
    <property name="RBEL_LOG_LEVEL" value="${RBEL_LOG_LEVEL:-WARN}"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>${CONSOLE_LOG_LEVEL}</level>
        </filter>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/testsuite.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/testsuite.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>${FILE_LOG_LEVEL}</level>
        </filter>
    </appender>

    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>2048</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>true</neverBlock>
        <appender-ref ref="FILE"/>
    </appender>

    <logger name="de.gematik.test.tiger" level="${TIGER_LOG_LEVEL}"/>
    <logger name="de.gematik.test.tiger.proxy" level="${TIGER_PROXY_LOG_LEVEL}"/>
    <logger name="de.gematik.rbellogger" level="${RBEL_LOG_LEVEL}"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_FILE"/>
    </root>
</configuration>
